#' Parameter documentation
#'
#'
#' This function is only here to gather the documentation of different parameters used
#' throughout the package.
#'
#'
#' @name documentation
#'
#' @param wrp_con [wrp_connect] object.
#'
#' @param class Selector for the GTS classes.
#' Can either be an exact match or a regular expression (the value should then start with a ~).
#'
#' @param endpoint egress endpoint.
#'
#' @param execute A boolean indicating if the warpScript should be exectuted or not.
#'
#' @param labels Map of labels selectors.
#' The key is the label name, the value a selector which can be an exact match or a regular expression
#' (the value should then start with a ~).
#'
#' @param attributes Map of attributes.
#'
#' @param token a token needed to connect to database
#'
NULL
